/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package web.action;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import common.exception.MotorbikeCarException;
import common.util.BeanFactory;
import dao.bean.ApplicationRecord;

import service.IMotorCarService;
import web.form.CheckAppForm;

/** 
 * MyEclipse Struts
 * Creation date: 05-10-2008
 * 
 * XDoclet definition:
 * @struts.action path="/checkApp" name="checkAppForm" input="/pages/car/endorsement.jsp" scope="request"
 * @struts.action-forward name="success" path="/pages/index.jsp" redirect="true"
 */
public class CheckAppAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		CheckAppForm checkform = (CheckAppForm) form;
		long id=checkform.getId();
		long status=checkform.getStatus();
		IMotorCarService service = (IMotorCarService) BeanFactory
				.getBean("motorcarService");
		try {
			ApplicationRecord record=service.findAppById(id);
			record.setStatus(status);
			service.updateAppRecord(record);
		} catch (MotorbikeCarException e) {
			e.printStackTrace();
			return null;
		}
		return mapping.findForward("success");
	}
}